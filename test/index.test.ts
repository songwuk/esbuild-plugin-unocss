import { describe, expect, it } from 'vitest'

describe('import', () => {
  it('.js', () => {
    expect(1).toEqual(1)
  })
  it('.jsx', () => {
    expect(1).toEqual(1)
  })
  it('.ts', () => {
    expect(1).toEqual(1)
  })
  it('.tsx', () => {
    expect(1).toEqual(1)
  })
})
